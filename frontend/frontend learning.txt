first define project structure
navbar tag and component cant be used simultaneously because of same name
use header or div
use location hook react
::-webkit-scrollbar {
  display: none;
}

redux setup
async thunk
local signup states
value assign
form data
dispatch
Hooks cannot run outside the component body. Always inside the function.
cors origin in backend
import.meta.env.BACKEND_PORT to use .env in frontend
process.env to use .env in backend
new cookieOptions made in backend
axios file made in frontend

.unwrap()

This is important.
If logout succeeds → continues
If logout fails → throws an error
api.put

understand .wrap, reducers and extra reducers
test token time

videoURL?.url
“If videoURL exists, access .url; otherwise return undefined instead of crashing.

videoURL.url
If videoURL is null or undefined → runtime error.

thumbnailUrl = videoURL?.url
      .replace("/upload/", "/upload/so_1,w_600,c_fill/")
      .replace(".mp4", ".jpg");

{ new: true } ensures updatedVideo is the updated document, not the old one.

Summary – in very simple words
This code:

Catches “401 – you are logged out” errors
Tries to silently get a new login token
If successful → retries your original request
If many requests fail at once → only one refresh happens, others wait
If refresh fails → logs you out
Prevents you from seeing “please login again” popups every few seconds